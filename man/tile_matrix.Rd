% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{tile_matrix}
\alias{tile_matrix}
\title{Tile a numeric matrix to a nativeRaster pattern}
\usage{
tile_matrix(x, width, height)
}
\arguments{
\item{x}{A numeric matrix. Interpreted as a single-channel pattern.}

\item{width, height}{Integers. Output image width and height in pixels.}
}
\value{
A \code{nativeRaster} of dimensions \code{height * width}.
}
\description{
Repeats a numeric matrix \code{x} to fill an image of size
\code{width} by \code{height}, then returns it as a grayscale \code{nativeRaster}
(RGB channels are identical; alpha is set to 255).

This is mainly intended to build threshold/texture maps for effects such as
\code{\link[=screen_tone]{screen_tone()}}, where small matrices (e.g. Bayer matrices or custom kernels)
are tiled across an image.

Values are clamped to \verb{[0, 255]} before being packed.
}
\examples{
# 4x4 Bayer matrix, scaled to 0..255
b4 <- kernel_bayer(4, normalize = FALSE) * 255
\dontrun{
pat <- tile_matrix(b4, width = 320, height = 180)
grid::grid.newpage()
grid::grid.raster(pat, interpolate = FALSE)
}
}
