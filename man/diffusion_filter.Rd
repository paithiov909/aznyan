% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{diffusion_filter}
\alias{diffusion_filter}
\title{Diffusion-based smoothing and enhancement}
\usage{
diffusion_filter(
  nr,
  factor = 5,
  offset = 0.1,
  iter = 3,
  gamma = 1.3,
  sigma = 2
)
}
\arguments{
\item{nr}{A \code{nativeRaster} object.}

\item{factor}{A numeric scalar controlling the decay factor used at each
iteration. Larger values reduce the contribution of later diffusion steps.}

\item{offset}{A numeric scalar added to the iteration index when computing
the decaying gain. Helps adjust the early-iteration weighting.}

\item{iter}{An integer scalar giving the number of diffusion iterations to
perform. More iterations strengthen the smoothing and glow effects.}

\item{gamma}{A numeric scalar specifying the gamma exponent applied before
diffusion (and inverted afterward). Values greater than \code{1} emphasize
bright regions.}

\item{sigma}{An integer scalar giving the initial Gaussian blur radius
(converted to a standard deviation internally). The value is squared on
each iteration, producing progressively wider diffusion.}
}
\value{
A \code{nativeRaster} object.
}
\description{
Applies an iterative diffusion-style filter to a \code{nativeRaster} image.
This custom process repeatedly blurs a gamma-transformed version of the
image and accumulates the diffused values with a decaying gain factor.
The result is then inverse–gamma–corrected, producing a soft, glowy,
detail-enhancing effect reminiscent of multi-scale diffusion or
photographic bloom.
}
